# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Murphy — restore points / backups from `fix` (never commit host state)
.murphy-backups/
*.murphy.bak

# Editors / OS cruft
.vscode/
.idea/
*.swp
.DS_Store

# Built Magisk module - a build output, not source. Ship it as a release asset
# rather than tracking it, or every rebuild adds another copy to history.
murphy-hardening.zip
murphy-hardening.zip.*
